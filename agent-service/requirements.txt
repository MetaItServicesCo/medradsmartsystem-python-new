# Agent microservice dependencies.
#
# Deliberately isolated from backend/requirements.txt: LangGraph requires a
# modern pydantic while the MedRad backend is pinned at 2.5.0. Keeping the two
# dependency sets apart is the main practical benefit of running the agent as
# its own service — the backend needs no upgrade.
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.5
pydantic-settings==2.15.0
sse-starlette==3.4.8

anthropic==1.3.0
langgraph==1.2.11
langchain-core==1.6.1
